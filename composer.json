{
    "name": "tflori/secure-token",
    "description": "Library to generate tokens of arbitrary precision",
    "license": "MIT",
    "readme": "README.md"
    "require": {
        "php": ">=5.3",
        "ext-bcmath": "*"
    },
    "autoload": {
        "psr-4": {
            "SecureToken\\": "SecureToken/",
            "SecureToken\\Tests\\": "tests/"
        }
    },
    "require-dev": {
        "phpunit/phpunit": "^5.6"
    },
    "scripts": {
        "test": "phpunit",
        "test-wcc": "phpunit --coverage-html coverage"
    }
}
